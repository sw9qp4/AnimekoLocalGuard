# G. 工作日志

按时间顺序记录实际做了什么。**只写执行过的事**，不写计划。
每条的"结果"都是当次实际观察到的输出。

---

## G.1 阶段一：G0 迁移与基线（早期轮次）

| 动作 | 结果 |
|---|---|
| 从 `D:\模型\BiliLocalGuard` 迁移到新工程 | 旧工程未改动 |
| 恢复总任务说明文件 | SHA-256 `4a3aa137…`、33733 字节、317 行，三项与记录一致 |
| 锁定上游 | HEAD = `28ec14ac…`，`git diff` 确认工作树未偏离 |
| 建立 ASCII junction 构建路径 | `D:\alrepo` → 仓库，`D:\al-tools` → `.tools` |
| 解析构建阻塞（9 次尝试） | 根因：非 ASCII 路径 + Gradle `@argfile` 传测试 worker classpath |
| 首次构建成功 | 产出 APK，applicationId `me.him188.ani.localguard` |
| 静态确证接入点 | 全应用**只有 2 个**真实弹幕文字显示入口 |

---

## G.2 阶段二：G1 纯逻辑与接线（多轮）

| 动作 | 结果 |
|---|---|
| 实现三档策略 | 单调性 + 两两可区分 + 方向性 由属性测试钉住（>1000 输入） |
| 实现揭晓时间线规则 | 用上界解锁；未映射区域不插值 |
| 实现错误状态 | 发现 `GuardDecision.Failed` 曾是**不可达死代码**，已修 |
| 实现两级缓存 | 发现通用层剥离剧透分数会**破坏缓存可靠性**，已修 |
| 实现剧情包格式与装载 | 发现 `KnowledgePackValidator` **从未在装载时被调用**，已修 |
| 实现剧情包生成流程 | 人手声明 + 带时间字幕 → 推导时间 |
| 实现片源对齐生成 | 发现常量偏移判据用**最大值**而非 90 分位，已改为分位 |
| 实现端到端管线测试 | 覆盖 字幕→包→落盘→装载→对齐→判定 |
| 接入两个显示入口 | 覆盖层 + 弹幕列表 |
| 界面开关与状态行 | 11 条文案 × 7 个语言目录 |

### 本阶段发现并修复的"实现了但没接线"（共 5 次）

1. 缓存类写好了但生产路径从不创建
2. `GuardDecision.Failed` 不可达
3. `knowledge` / `alignment` 参数从未传进 `startEpisode`
4. `modelFailed` 从未在生产路径被设置
5. `KnowledgePackValidator` 从未被装载路径调用

**这 5 次都不是单元测试发现的，而是靠死代码审计或端到端测试发现的。**

---

## G.3 阶段三：素材实测（第 10 轮）

| 动作 | 结果 |
|---|---|
| 抓取 Bangumi 高分 TV 动画 | 500 条候选池（评分 6.8–9.9） |
| 实测弹幕源可达性 | Animeko 自有源可达；DanDanPlay 需凭据（`errorCode 3`） |
| 实测自有源覆盖率 | **极低**：多数作品每集个位数，部分为 0 |
| 查明"带时间字幕拿不到" | 不在任何 API 里 → **G2 的真正瓶颈** |
| 纠正一个错误结论 | 早期"两个 APK 字节完全相同"是**错的**（CRC32 对 ZIP64 不可靠） |

---

## G.4 阶段四：短片片单（第 15–16 轮）

| 动作 | 结果 |
|---|---|
| 按用户要求收敛到短片 | 85 部 ≤14 集、评分 ≥8.0 |
| 逐部实测弹幕（每部前 4 集） | 最高 19.5 条/集（魔法少女小圆），4 部全为 0 |
| 核对时间单位 | `playTime` 是**毫秒**，上游原样透传，无换算错误 |
| 补齐 3 份缺失的示例字幕 | `docs/DECLARATION_FORMAT.md` 一直在引用**不存在的文件** |
| 把示例字幕接进测试 | 钉住 F_IDENTITY/… 解析出的**具体毫秒** |
| 固化 DanDanPlay 签名算法 | `base64(sha256(appId+ts+path+secret))` |

---

## G.5 阶段五：修复集序接线缺陷（第 16 轮）

| 动作 | 结果 |
|---|---|
| 死代码审计报出 `EpisodeOrderMapper` 只有测试在用 | 确认生产路径**从未使用** |
| 定位真实错误 | `ep`（当季集数）与 `sort`（系列集数）被混用；特别篇编号被当正片集数 |
| 新增适配层 `LocalGuardEpisodeOrder` | 18 例测试，全部用上游真实工厂构造 |
| 新增 APK 核验工具 | 从产物直接读包名/版本/签名/资产/ABI |
| 构建并核验 | debug APK `sha256 11c57a5a…`，`assets/story` 为 **0** |

---

## G.6 阶段六：真机准备（第 17 轮）

| 动作 | 结果 |
|---|---|
| 检查设备 | `adb devices` 空列表 → **无设备** |
| 编写 `tools/device_verify.ps1` | 默认只读；写入需显式开关 |
| 编写 `docs/G1_DEVICE_CHECKLIST.md` | 7 项手动核查项 + 记录要求 |
| 空跑验证脚本 | 正确输出 `No device detected`，exit=3 |
| 上游改动面终核 | 12 个文件被改，**共删除 5 行**，逐行核对为无害重排 |

---

## G.7 阶段七：自有签名与发布（第 17 轮）

| 动作 | 结果 |
|---|---|
| 生成自有签名密钥库 | RSA 4096 / SHA384withRSA，证书 `f92b1078…` |
| 确认与 debug 密钥不同 | debug 为 `966fba3c…`（**同 DN 也可能是不同密钥，必须比指纹**） |
| 构建 release（含 minify） | BUILD SUCCESSFUL，37,011,781 字节 |
| 核验 release 签名 | 证书 `f92b1078…` ✅ |
| **核验 release 包名** | ❌ **`me.him188.ani`** ← 与官方冲突，**见 D-1** |
| 修复包名后缀 | 已改 `app/android/build.gradle.kts` + `local.properties` |
| 修复后重建 | ❌ **未做**（构建环境在此期间被破坏） |

---

## G.8 阶段八：发布到 GitHub

| 动作 | 结果 |
|---|---|
| 编写 `tools/secret_scan.js` | 扫描"git 会发布的那批文件" |
| 首次扫描 | 46,213 个问题（全是 `.tools/`，说明 `.gitignore` 有缺口） |
| 收紧 `.gitignore` | 排除 `.tools/`、`artifacts/`、`docs/evidence/` 等 |
| 再扫描 | 12 个问题，全部为误报（VLC 的 keystore **插件**、属性名而非值） |
| 收紧匹配规则 | 只匹配**带字面值**的凭据 |
| 三次扫描 | **0 问题** |
| 发现 PowerShell 破坏 UTF-8 | 44 个非 ASCII 文件名被 git 转义成 `\nnn` → 改用 `git ls-files -z` 写文件 |
| 发现上游 gitignore 排除中文翻译 | 4 个改了文案的 `strings.xml` 会被静默丢弃 → `git add -f` |
| 创建仓库 | https://github.com/sw9qp4/AnimekoLocalGuard （public） |
| 推送 | 3,840 个文件，单个 import 提交 `0de136f` |
| 发布后逐路径核对 | 密钥/凭据目录**不可访问**，源码**可访问** |

---

## G.9 阶段九：外部审阅材料（本轮）

| 动作 | 结果 |
|---|---|
| 核准全部数字 | 重新跑测试汇总、APK 核验、产物清单 |
| 编写 7 份审阅文档 | 见 `docs/audit/` |
| 打包 | 单一可粘贴文件（1,277 行 / 69 KB）+ zip（130,070 字节） |
| 发布审阅材料 | 提交 `111e39b`，已推送 |
| 验证远端 | `git ls-remote` 与本地 HEAD 一致 |

---

## G.10 本轮承认的操作失误

| # | 失误 | 后果 | 已做的补救 |
|---|---|---|---|
| 1 | 用中文写 `.ps1` | 脚本根本无法解析（PowerShell 5.1 按 GBK 读无 BOM 文件） | 改为纯 ASCII 脚本，中文放 `.md` |
| 2 | 清 Gradle 缓存清过头 | 破坏构建环境（`Unresolved reference 'plugins'`） | 已恢复；写下正确的清理顺序（D-10） |
| 3 | 删除 `GRADLE_USER_HOME/caches` 整个目录 | 需重新下载依赖，耗时 | 已恢复 |
| 4 | 起初用 PowerShell 捕获 `git ls-files` | 44 个非 ASCII 文件名被误判为"不存在" | 改用 `git ls-files -z` |
| 5 | `aapt2 dump strings` 用来找文案 | 永远搜不到（那是**文件名**池，不是值池） | 改写 `resources.arsc` 解码器 |
| 6 | 一度把 `.scan-filelist.bin` 提交进仓库 | 无敏感内容，但属脏文件 | 下一个提交已删除并加入 `.gitignore` |

---

## G.11 时间线概览

```
G0 迁移 ──► 构建打通 ──► G1 纯逻辑 ──► 接线（2 个入口）
                                      │
                                      ├──► 素材实测（发现瓶颈是字幕）
                                      │
                                      ├──► 短片片单 + 弹幕实测
                                      │
                                      ├──► 修复集序接线缺陷
                                      │
                                      ├──► 自有签名 + release 构建（发现包名缺陷）
                                      │
                                      └──► 发布到 GitHub + 外部审阅材料  ◄── 现在在这里
                                      
未走的路：真机验证（无设备）、真实剧情包（无字幕）、模型（未开始）
```
