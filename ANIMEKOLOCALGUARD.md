# AnimekoLocalGuard

**本仓库是 [Animeko](https://github.com/open-ani/animeko) 的一个非官方本地修改版**，目标是做
**本地、可关闭、按剧情时间线判断的弹幕剧透过滤**。

> ⚠️ 这不是官方 Animeko。请不要把本仓库的问题反馈给 Animeko 官方。

---

## 1. 它想解决什么

看番时最扫兴的往往不是剧情本身，而是**弹幕提前把后面的剧情说了**。
常规的关键词过滤挡不住这种内容，因为剧透常常不含任何"敏感词"：

> 「有老虚，不可能治愈」

这句话一个字都没提剧情，但它把整部作品的**预期**改掉了。这类弹幕只有结合
"观众此刻应该已经知道多少"才能判断该不该出现。

本项目的做法：

1. **人工声明事实**——由人写出"哪条事实、涉及谁、严重程度"，
   工具**不推断剧情**；
2. **用带时间轴的字幕定位揭晓时刻**——证据是**台词引用**，不是凭感觉写的秒数，
   因此任何时候都能重新核对；
3. **按播放位置判定**——同一条弹幕在同一集的不同时间点，结论可以不同
   （揭晓前拦、揭晓后放行）；
4. **完全在本地判断**，且**默认关闭**。

## 2. 与上游的关系

| 项目 | 值 |
|---|---|
| 上游 | https://github.com/open-ani/animeko |
| 基线提交 | `28ec14aca0a2b001f2ce1f6dd1b68acd44a14f67` |
| 许可证 | AGPL-3.0（见 `LICENSE.txt`） |
| 本仓库相对上游的改动 | 12 个受跟踪文件被修改（共删除 5 行，均为空行或参数列表重排），其余为新增文件 |

上游既有逻辑**没有被删除或改写语义**。改动集中在：新增 `danmaku/localguard` 模块、
在 `EpisodeViewModel` 的显示路径上接入判定、播放页弹幕设置里新增一个设置分组。

## 3. 包名与签名（重要）

自编译版使用**独立的包名**，因此与官方 Animeko **共存，不会覆盖**：

| 变体 | applicationId |
|---|---|
| 上游官方 | `me.him188.ani` |
| 本项目的 debug 版 | `me.him188.ani.localguard` |
| 本项目的 release 版 | `me.him188.ani.localguard` |

签名使用**本项目自有的密钥**（非官方 Animeko 发布密钥）。密钥库与密码
**不在本仓库中**，见 `.gitignore`。

## 4. 当前状态（请以此为准，不要读成"已完成"）

项目维护 `PROGRESS.md` / `NEXT.md` / `STATE.json`，并严格区分这些状态词：

`DESIGNED` / `IMPLEMENTED` / `STATIC_EVIDENCE` / `UNIT_TESTED` / `BUILT` / `DEVICE_VERIFIED` / `MODEL_EVALUATED` / `BLOCKED` / `UNKNOWN`

**已做到：**

- 三档策略（宽松/均衡/严格）、揭晓时间线规则、错误状态、缓存隔离、可关闭开关
  → 纯逻辑单元测试通过（292 例）；界面层 119 例
- 两个真实弹幕文字显示入口都已接入判定
- 剧情包格式、装载校验、生成工具、片源对齐生成、端到端管线测试
- 独立包名与自有签名的构建基线

**尚未做到（这是当前的真实边界）：**

- **没有内置任何剧情包**，也没有安装语义模型。因此
  **即使打开开关，也不会拦下任何弹幕。**
- **没有做过真机验证**（G1 的完成标志后半未达成）
- 未测量任何性能

换句话说：**链路是通的，内容是空的。** 这一点在 `PROGRESS.md` 与 `STATE.json` 中有完整记录。

## 5. 目录说明（本项目新增的部分）

| 路径 | 内容 |
|---|---|
| `danmaku/localguard/` | 纯逻辑核心：策略、时间线、缓存、知识包、对齐生成 |
| `app/shared/src/commonMain/kotlin/data/repository/danmaku/` | 配置持久化、集序适配 |
| `docs/` | 迁移决策、接入点证据、素材调研、声明格式、真机核查清单 |
| `tools/` | 构建脚本、设备核查脚本、以及若干自检工具 |
| `PROGRESS.md` / `NEXT.md` / `STATE.json` | 进度、下一步、机器可读状态 |

`docs/` 中的 `DeepSeek_Animeko_LocalGuard_Master_Prompt.txt` 是本项目的任务说明；
`docs/contributing/`、`docs/dev/` 等目录来自上游。

## 6. 许可与合规

- 本仓库整体遵循 **AGPL-3.0**（`LICENSE.txt`），与上游一致。
- 上游版权归 OpenAni 及贡献者所有；本项目的新增部分同样按 AGPL-3.0 发布。
- 仓库中**不包含**：视频、字幕文件、真实作品的弹幕数据、任何 API 凭据或签名密钥。
- 本项目的 DANMAKU 相关工具只做**计数**与**格式核对**，不批量抓取弹幕内容。

## 7. 免责

本项目仅供个人学习与本地观影使用。请遵守你所使用的一切数据源的服务条款。
