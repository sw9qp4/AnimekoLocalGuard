Record the external-review round in the maintained status files

PROGRESS.md gains the round entry for the review-package work: publishing to GitHub, the two
publishing pitfalls that would have failed silently (PowerShell mangling non-ASCII git output,
and upstream's own .gitignore excluding the Chinese translation files that carry the new UI
text), plus the two new defects found while verifying the artifacts.

STATE.json gains the externalReviewPackage block: where the material lives, what the eight
documents cover, the local packaging artifacts, and the seven questions being put to the
reviewer.

Both files already carried every earlier round, so this keeps the published repository and the
working tree in step.
